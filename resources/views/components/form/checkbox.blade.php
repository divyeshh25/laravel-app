<div class="col-8">
    <div class="icheck-primary">
        <input type="{{ $type }}" id="{{ $name }}" name="{{ $name }}">
        <label for="{{ $name }}">
            Remember Me
        </label>
    </div>
</div>